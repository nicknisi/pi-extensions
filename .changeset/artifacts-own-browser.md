---
'@nicknisi/pi-artifacts': patch
---

Block `launch_browser`/`navigate_browser` calls aimed at the artifact server so artifacts stay in the user's default browser and profile instead of a managed test browser.
