---
'@nicknisi/pi-self-compact': patch
'@nicknisi/pi-statusline': patch
---

Color the existing statusline context bar using self-compaction thresholds. Hide the duplicate self-compact widget when the statusline is active, while preserving standalone fallbacks and the footer's existing remaining-context display.
