---
'@nicknisi/pi-checkpoint': patch
'@nicknisi/pi-statusline': patch
---

Color the existing statusline context bar using self-compaction thresholds. Hide the duplicate Checkpoint widget when the statusline is active, while preserving standalone fallbacks and the footer's existing remaining-context display.
