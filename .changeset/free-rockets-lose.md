---
'@nicknisi/pi-codemode': patch
'@nicknisi/pi-workflows': patch
---

Consolidate orchestration guidance into tool descriptions instead of duplicating it in global prompt guidelines. Clarify output contracts, parallel callbacks, shared-tree serialization, and workflow agent return values without changing execution behavior.
