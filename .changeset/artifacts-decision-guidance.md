---
'@nicknisi/pi-artifacts': patch
---

Prompt guidance now tells the agent when to attach `decisions`: whenever an artifact presents options or trade-offs the user must choose between, instead of asking in the terminal. The tool snippet also mentions collecting choices.
