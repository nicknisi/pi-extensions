---
'@nicknisi/pi-btw': minor
---

Make the `/btw` side-chat model configurable through `~/.pi/agent/configs/btw.json`, using a `provider/model-id` string and preserving `fireworks/glm-latest` as the default.
